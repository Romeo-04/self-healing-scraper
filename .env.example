# Bright Data Scraper Studio
BRIGHT_DATA_API_TOKEN=
BRIGHT_DATA_COLLECTOR_ID=

# Fallback heal agent (primary heal path uses Bright Data's own AI via the CLI)
OPENAI_API_KEY=
OPENAI_MODEL=

# Public URL the collector uses to reach the mutable mirror
# Set from: cloudflared tunnel --url http://localhost:3000
MIRROR_PUBLIC_URL=
